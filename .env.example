# ============================================================
#  Test-Sync-Pro  –  Environment Configuration
# ============================================================
# Copy this file to  .env  and fill in your real values.
# NEVER commit the .env file to source control.
# ============================================================

# ---------- Azure DevOps Settings ----------
# Organization URL  (e.g. https://dev.azure.com/your-org)
ADO_ORG_URL=https://dev.azure.com/YOUR_ORGANIZATION

# Project name exactly as it appears in ADO
ADO_PROJECT=YOUR_PROJECT_NAME

# Personal Access Token – required scopes:
#   • Work Items  (Read & Write)
#   • Test Management (Read & Write)
ADO_PAT=YOUR_PERSONAL_ACCESS_TOKEN

# Test Plan ID where suites/folders will be created
ADO_TEST_PLAN_ID=

# ---------- LLM Settings ----------
# Set LLM_PROVIDER to choose which AI service to call DIRECTLY.
# Each provider uses its own native SDK — no proxy, no middleman.
#
# Supported values for LLM_PROVIDER:
#   openai        → OpenAI  (default)
#   anthropic     → Anthropic Claude  (native SDK)
#   azure_openai  → Azure OpenAI
#   groq          → Groq           (OpenAI-compatible)
#   deepseek      → DeepSeek       (OpenAI-compatible)
#   mistral       → Mistral        (OpenAI-compatible)
#   together      → Together AI    (OpenAI-compatible)
#   google        → Google Gemini  (OpenAI-compatible endpoint)
#   ollama        → Ollama local   (OpenAI-compatible)
#   lmstudio      → LM Studio local(OpenAI-compatible)
#   custom        → Any other OpenAI-compatible API (set LLM_BASE_URL)
#
# ┌──────────────┬─────────────────┬─────────────────────────┐
# │ LLM_PROVIDER │ LLM_API_KEY     │ Example LLM_MODEL       │
# ├──────────────┼─────────────────┼─────────────────────────┤
# │ openai       │ sk-...          │ gpt-4o                  │
# │ anthropic    │ sk-ant-...      │ claude-sonnet-4-20250514│
# │ azure_openai │ (use vars below)│ (use vars below)        │
# │ groq         │ gsk_...         │ llama-3.3-70b-versatile │
# │ deepseek     │ sk-...          │ deepseek-chat           │
# │ mistral      │ ...             │ mistral-large-latest    │
# │ together     │ ...             │ meta-llama/Llama-3-70b  │
# │ google       │ AIza...         │ gemini-2.0-flash        │
# │ ollama       │ ollama          │ llama3                  │
# │ lmstudio     │ lm-studio       │ (auto-detected)         │
# │ custom       │ ...             │ ...                     │
# └──────────────┴─────────────────┴─────────────────────────┘

LLM_PROVIDER=openai
LLM_API_KEY=YOUR_API_KEY
LLM_MODEL=gpt-4o

# Only needed for LLM_PROVIDER=custom  (your own OpenAI-compatible URL)
# LLM_BASE_URL=

# ---------- Optional: Azure OpenAI ----------
# Only needed when LLM_PROVIDER=azure_openai
# AZURE_OPENAI_ENDPOINT=https://YOUR_RESOURCE.openai.azure.com/
# AZURE_OPENAI_API_KEY=YOUR_AZURE_OPENAI_KEY
# AZURE_OPENAI_DEPLOYMENT=YOUR_DEPLOYMENT_NAME
# AZURE_OPENAI_API_VERSION=2024-02-15-preview

# ---------- Agent Behaviour ----------
# Similarity threshold for de-duplication (0.0 – 1.0, default 0.90)
DEDUP_THRESHOLD=0.90
